import api from '../api';
import * as types from '../types';

const state = {
};
const actions = {

};
const getters = {

};
const mutations = {

};

export default {
  state,
  actions,
  getters,
  mutations
}
